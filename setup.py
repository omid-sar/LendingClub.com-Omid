from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Prediction of Borrower Behavior(eligible or not/ If eligible how to pay off the loan)',
    author='OmidSar',
    license='',
)
